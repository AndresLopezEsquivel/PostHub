export function Explore() {
  // docs/screens.md §3 — Explore, all posts. Public; engagement actions require auth.
  // TODO(pass 2): GET /api/posts with search/category/sort/page/limit bound to URL
  // search params, rendered through the shared <PostCard>. Establishes the
  // loading/empty/error triad every later list reuses.
  return (
    <section>
      <h1>Explore</h1>
      <p>Not implemented yet.</p>
    </section>
  );
}
