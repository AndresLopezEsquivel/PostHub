export function Feed() {
  // docs/screens.md §4 — personalized feed. Authenticated only.
  // TODO(pass 7): GET /api/feed (page/limit only). The empty state is a call to
  // action ("follow someone to see posts here"), NOT "no posts exist" — that
  // distinction is the whole reason this screen is separate from Explore.
  return (
    <section>
      <h1>Feed</h1>
      <p>Not implemented yet.</p>
    </section>
  );
}
