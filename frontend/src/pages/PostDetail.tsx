export function PostDetail() {
  // docs/screens.md §5 — Post detail. Public to read.
  // TODO(pass 3): GET /api/posts/:postId (returns <postCard> + content, imageKey,
  // updatedAt) and GET /api/posts/:postId/comments (oldest-first). 404 renders a
  // "post not found" state on this screen, not the NotFound route.
  return (
    <section>
      <h1>Post</h1>
      <p>Not implemented yet.</p>
    </section>
  );
}
