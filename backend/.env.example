PORT=4000
NODE_ENV=development
DATABASE_URL=postgresql://posthub:posthub@localhost:5432/posthub

# TLS to the database. Leave false for local Postgres (Compose or host); set
# true for managed databases like RDS that require encrypted connections.
DATABASE_SSL=false
