import { Router } from 'express';

// Mounted at /api/notifications — see docs/api_design.md "Notifications"
// GET /, PATCH /:notificationId, POST /read-all
const router = Router();

export default router;
